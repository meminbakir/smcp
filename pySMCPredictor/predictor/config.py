import os
PROJECT_ROOT = os.path.abspath(os.path.dirname(__file__))
DATA_DIR = PROJECT_ROOT + os.sep + "data" + os.sep
TEST_DATA_DIR = PROJECT_ROOT + os.sep + "test" + os.sep + "data" + os.sep
LOG_DIR = PROJECT_ROOT + os.sep + "results" + os.sep
